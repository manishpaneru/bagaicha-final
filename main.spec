# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('cafe_manager/utils/*.py', 'cafe_manager/utils'),
        ('cafe_manager/pages/*.py', 'cafe_manager/pages'),
        ('cafe_manager/*.py', 'cafe_manager'),
    ],
    hiddenimports=[
        'customtkinter',
        'tkcalendar',
        'sqlite3',
        'PIL',
    ],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(
    a.pure,
    a.zipped_data,
    cipher=block_cipher
)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='Tropical Bagaicha',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='cafe_manager/assets/icon.ico' if os.path.exists('cafe_manager/assets/icon.ico') else None,
) 